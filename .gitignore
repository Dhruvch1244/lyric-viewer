node_modules/
dist/
dist-installer/
out/
*.log
.DS_Store

# Machine-specific Claude Code local settings
.claude/settings.local.json

# Shipped API keys — bundled into the installer, never committed
src/main/secrets.js

# Promo stills are regenerated, not authored — keep them out of git.
promo/stills/
